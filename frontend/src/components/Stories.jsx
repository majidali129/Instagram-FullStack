import Story from "../shared/Story";

const Stories = () => {
  return (
    <ul className="flex items-center space-x-4 min-w-[375px] h-full">
      <Story user={{}} />
      <Story user={{}} />
      <Story user={{}} />
    </ul>
  );
};

export default Stories;
